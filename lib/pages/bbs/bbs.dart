import 'package:flutter/material.dart';

/**
 * 主页动态tab页
 */
class BBSPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new ListView(
      children: [new Text('BBS')],
    );
  }
}